//
//  ContentView.swift
//  Cashew
//
//  Created by Cédric Bahirwe on 22/02/2026.
//

// ContentView.swift is no longer used as the root.
// The app entry point (CashewApp.swift) launches HomeView directly.
// This file is kept to avoid removing it from the Xcode project manually —
// you can safely delete it from Xcode if you wish.

import SwiftUI

struct ContentView: View {
    var body: some View {
        HomeView()
    }
}
